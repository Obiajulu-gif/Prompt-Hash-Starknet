# Core dependencies
fastapi
uvicorn
pydantic
python-dotenv
python-multipart
starlette
typing-extensions
requests
request-limiter

# Secret Network SDK (PINNED to avoid prerelease deps)
secret-sdk==1.8.1
secret-ai-sdk

# Deployment
gunicorn
